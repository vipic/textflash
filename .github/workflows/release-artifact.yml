name: Release Artifact

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Release version, for example 0.1.12"
        required: true

permissions:
  contents: read

jobs:
  build-dmg:
    name: Build DMG
    runs-on: macos-15
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2
        with:
          fetch-depth: 0

      - name: Build Release DMG
        run: ./release.sh "${{ inputs.version }}" --force
        # --force: CI checkout 是干净的，跳过 dirty-tree 检查；tag/branch 保护在本地发布流程中处理

      - name: Validate DMG
        run: |
          set -euo pipefail
          DMG="dist/TextFlash-${{ inputs.version }}.dmg"
          test -s "$DMG"

          MOUNT_OUTPUT=$(hdiutil attach -readonly -noverify -noautoopen "$DMG")
          DEVICE=$(echo "$MOUNT_OUTPUT" | awk '/\/Volumes\// {print $1; exit}')
          VOLUME=$(echo "$MOUNT_OUTPUT" | awk '/\/Volumes\// {for (i=3; i<=NF; i++) printf "%s%s", (i==3 ? "" : " "), $i; print ""; exit}')
          trap 'hdiutil detach "${DEVICE:-$VOLUME}" -quiet' EXIT

          APP="$VOLUME/TextFlash.app"
          test -d "$APP"
          VERSION=$(/usr/libexec/PlistBuddy -c "Print :CFBundleShortVersionString" "$APP/Contents/Info.plist")
          test "$VERSION" = "${{ inputs.version }}"
          codesign --verify --deep --strict "$APP"

      - name: Upload DMG
        uses: actions/upload-artifact@b4b15d8c7c6ac21ea08fcf65892d2ee8f75cf882  # v4.4.3
        with:
          name: TextFlash-${{ inputs.version }}.dmg
          path: dist/TextFlash-${{ inputs.version }}.dmg
          if-no-files-found: error
