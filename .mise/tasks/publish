#!/usr/bin/env bash
set -euo pipefail

if [[ "$#" -eq 0 || "$1" == --* ]]; then
    echo "正式发布必须显式指定版本：mise run publish -- <x.y.z>" >&2
    exit 2
fi
VERSION="$1"
shift

echo "Publishing version: $VERSION"
exec ./release.sh "$VERSION" --publish "$@"
